---
title: 发送策略链路
tags: strategy, send
---

# 发送策略链路

Java 发送链路通常先初始化上下文，再执行限流和发送策略校验，然后做参数校验、接收人填充、模板数据填充、消息过滤、按通道发送，最后更新发送状态。这个顺序决定了排查时要先确认消息有没有进入发送上下文，再看策略、模板和通道。

频控建议按租户、通道、接收人或模板维度隔离 key，优先使用 Redis 计数；没有 Redis 时可以使用 SQLite fallback，但只能作为单机或开发环境兜底。策略命中时应该返回明确原因，方便区分“业务不该发”和“通道发不出去”。
